import SwiftUI

struct FindTabView: View {
    var body: some View {
        Text("Find")
            .font(.largeTitle)
            .fontWeight(.black)
            .frame(width: 250.0, height: 250.0)
            .background(.green.opacity(0.3))
    }
        
}
